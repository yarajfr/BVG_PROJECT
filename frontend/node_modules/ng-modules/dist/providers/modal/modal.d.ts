import { AlertOptions, ConfirmOptions, ToastOptions, InfoConfirmOptions, ModalOptions } from '../../components';
import { UtilProvider } from '../util/util';
import { I18nProvider } from '../i18n/i18n';
export declare class ModalProvider {
    private util;
    private i18n;
    constructor(util: UtilProvider, i18n: I18nProvider);
    alert(options: AlertOptions | string): Promise<any> & {
        destroy: () => void;
    };
    confirm(options: ConfirmOptions | string): Promise<any> & {
        destroy: () => void;
    };
    inforConfirm(options: InfoConfirmOptions | string): Promise<any> & {
        destroy: () => void;
    };
    loading(content: string, options?: ModalOptions): Promise<any> & {
        destroy: () => void;
    };
    modal(component: any, data?: any, options?: ModalOptions): Promise<any> & {
        destroy: () => void;
    };
    toast(content: string | ToastOptions): void;
}
