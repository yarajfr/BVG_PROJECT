import { AnimationBuilder } from '@angular/animations';
export declare class AnimationProvider {
    private animationBuilder;
    constructor(animationBuilder: AnimationBuilder);
    bounceIn(element: HTMLElement): Promise<{}>;
    pulse(element: HTMLElement): Promise<{}>;
    shake(element: HTMLElement): Promise<{}>;
    fadeOut(element: HTMLElement): Promise<{}>;
    fadeIn(element: HTMLElement): Promise<{}>;
}
