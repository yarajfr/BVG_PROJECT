import { ViewContainerRef, TemplateRef, Renderer2, ElementRef } from "@angular/core";
export declare class TabContentDirective {
    viewContainer: ViewContainerRef;
    templateRef: TemplateRef<any>;
    renderer: Renderer2;
    elementRef: ElementRef;
    inited: boolean;
    openStatus: boolean;
    constructor(viewContainer: ViewContainerRef, templateRef: TemplateRef<any>, renderer: Renderer2, elementRef: ElementRef);
    name: string;
    getRootElement(): HTMLElement;
    onopen(isopen?: boolean): void;
    open(): void;
    onclose(): void;
    close(): void;
}
