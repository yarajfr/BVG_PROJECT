import { Injectable } from "@angular/core";
import { PopoverController, PopoverConfig } from "./popoverController";

@Injectable()
export class PopoverProvider {
	constructor() { }

	create(config: PopoverConfig & {
		parent: HTMLElement,
	}) {
		return new PopoverController(config, config.parent);
	}
}